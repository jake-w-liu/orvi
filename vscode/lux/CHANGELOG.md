# Changelog

## 0.1.0

- Add `.lux` language registration.
- Add syntax highlighting, snippets, and language configuration.
- Add diagnostics via `lux check --json`.
- Add completions for Lux components, semantic prefixes, modifiers, and metadata.
- Add `Lux: Preview` side-by-side preview panel via `lux build`.
